@page
@model Milujeme_plarka.Pages.Quests.EditModel
@{
    ViewData["Title"] = "Edit";
}

<<h2>změjé= úkol ale pamatuj moc to neměň</h2>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="@Model.Quest.QuestId" />
            <div class="form-group">
                <label asp-for="@Model.Quest.QuestQuote"></label>
                <input asp-for="@Model.Quest.QuestQuote" class="form-control" />
                <span asp-validation-for="@Model.Quest.QuestQuote" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary">Uložit</button> <a class="btn" asp-page="Index">Zpět</a>
        </form>
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}

